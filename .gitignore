/bin/
/dist/
/target/
*.test
*.out
.DS_Store
__pycache__/
*.pyc
.idea/
.vscode/
# Legacy: older versions of the update workflow staged upstream lists here
# before merging. The current script streams everything in-memory; this entry
# is kept defensively so a stray local download never leaks into a commit.
/.upstream/
